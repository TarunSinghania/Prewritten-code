//0(2^n*n*n)time
//2^n*n space
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fr(i,a,b) for(int i =a ; i < b; i++)
#define mkp make_pair 
#define pb push_back
#define speed   ios::sync_with_stdio(false), cin.tie(0) , cout.tie(0)
#define sz 500;
#define ff first
#define ss second
#define MAX 10005
#define pr(a,n) fr(i,0,n){cout<<a[i]<<" ";}cout<<endl;
#define MAXBLOCKS 10000
#define all(x) x.begin(),x.end()
#define INF 10000000
#define MOD 1000000007
#define prm(m,r,c) fr(i,0,r){fr(j,0,c){cout<<m[i][j]<<" "; }cout<<endl;}
#define fillm(m,r,c,k) fr(i,0,r)fr(j,0,c)m[i][j]=k;
#define endl '\n'
#define MAXVERTICES 16
#define INF_COST 2e18
#define prv(v) for(auto s: v){cout<<s<<" ";}cout<<endl; 
//CHANGE MAXVERTICES ACCORDINGLY
int n,m;
ll dp[1<<MAXVERTICES][MAXVERTICES];
//Let dp[mask][i] be the number of Hamiltonian walk in the subgraph generated by vertices in mask, that ends in the vertex i but we set here starting at vertex 1
int g[MAXVERTICES][MAXVERTICES];
int d[MAXVERTICES][MAXVERTICES];

void init(){
  for(int i  = 0 ; i < (1<<n) ; i++){
    fr(j,0,n)
      dp[i][j]=0;
  }
  // fr(i,0,n)
  // dp[(1<<i)][i]=0;
}

main(){
  cin>>n>>m;
    fr(i,0,m)
    {
      int a,b,x;
      cin>>a>>b>>x;
      a--;b--;
      g[a][b]=1;g[b][a]=1;
      d[a][b]=x;d[b][a]=x;
     }

     init();  
     //Since we don't care at which vertex the cycle starts, assume that it starts at 0.
     dp[1][0]=1;

     for(int mask = 1; mask < (1<<n) ; mask++){
        
        for(int i  = 0 ; i <n ; i++)
        {
          if(!(mask&(1<<i)))continue;
        
          int prev_mask = mask^(1<<i);
          for(int j =0 ; j < n ; j ++)
          {
            if(g[j][i])
            dp[mask][i]+= dp[prev_mask][j];
           }
         
        }
      }
     ll ans = 0 ;
     for(int i = 0 ; i < n ; i++){
        if(dp[(1<<n)-1][i] != INF_COST && g[i][0]==1)
          ans+=dp[(1<<n)-1][i];
     }
     //every cycle counted twice check ex
//      5 6
// 1 2 1
// 2 3 5
// 3 4 2
// 4 5 6
// 2 5 2
// 3 1 1

     cout<<ans/2<<endl;
}